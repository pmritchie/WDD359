import React from 'react';
import Navigation from '../nav/Nav';
import './Header.css';

const Header = props => (
  <header className="header">
    <Navigation />
  </header>

);

export default Header;
